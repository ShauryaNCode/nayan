# Path: OfflineFaceAuth/src/components/overlay/LivenessRing.tsx
# Purpose: Skia animated ring overlay with color transitions per FSM state: blue(idle), green(detected), amber(challenge), red(fail), green(pass).
